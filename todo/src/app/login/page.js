import LoginModal from "../component/loginModal";
export default function Page() {

    return (
        <main className="min-h-full min-w-full flex justify-center items-center">
            <LoginModal/> {/* This is the login dialog box */}
        </main>
    );
}